<nav class="flex items-center gap-5">
    <x-nav.nav-link text="Home" href="{{ route('home') }}" />
    <x-nav.nav-link text="Get started" href="{{ route('home') }}" />
    <x-nav.nav-link text="About" href="{{ route('home') }}" />
    <x-nav.nav-link text="Contact" href="{{ route('home') }}" />
</nav>
